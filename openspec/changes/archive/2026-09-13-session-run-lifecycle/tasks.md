## 1. 类型与协议基础

- [x] 1.1 在 `shared/types.py` 的任务阶段中新增 `TASK_CANCELLED`，运行 `uv run pytest tests/unit/test_types.py tests/unit/test_task.py` 通过
- [x] 1.2 在 `protocol/errors.py` 新增应用错误码 `SESSION_BUSY = -32004`，补充错误码相关单测并运行通过
- [x] 1.3 在 `docs/protocol.md` 错误码表补充 `-32004 SESSION_BUSY`，人工核对描述准确

## 2. Run 模型与会话状态

- [x] 2.1 新增 Run 实体（标识、所属会话、状态机、底层任务、发起方），保证终态唯一且重复取消为 no-op；新增单测覆盖全部状态转换
- [x] 2.2 扩展会话状态：持有单个活跃 Run 与临时会话标记，单测覆盖「至多一个活跃 Run」不变量
- [x] 2.3 将订阅返回的活跃任务改为在途 Run（完成/取消即移除），更新会话状态读取逻辑与对应单测

## 3. 连接生命周期重构

- [x] 3.1 解耦读循环：控制类请求内联处理、对话请求任务化；单测验证对话执行期间可读取并响应 ping
- [x] 3.2 断开检测与取消编排：EOF 后解除订阅，若会话零订阅则取消活跃 Run 并在非取消上下文记录终态；单测覆盖
- [x] 3.3 发送串行化：为连接加入发送锁与关闭标志，安全丢弃关闭后的发送；单测覆盖并发发送不交错
- [x] 3.4 服务端关闭流程在取消连接任务之前取消全部在途 Run 并落地终态；集成测试验证退出后无悬挂任务

## 4. 对话 Run 化

- [x] 4.1 将对话处理器包装为 Run，正常完成 / 失败 / 达到步数上限分别记录对应终态；单测覆盖三类终态
- [x] 4.2 取消路径不向会话历史追加轮次；单测验证取消后历史长度不变
- [x] 4.3 会话已有活跃 Run 时拒绝新对话并返回 `-32004`；单测覆盖拒绝且不影响已有 Run
- [x] 4.4 未携带 `session_id` 的对话使用服务端生成的临时会话，空置即销毁；单测覆盖创建与销毁

## 5. 端到端验证

- [x] 5.1 e2e：客户端在对话执行中断开，服务端立即取消并清理，断言会话无残留活跃 Run 与死订阅
- [x] 5.2 e2e：共享会话中一个订阅者断开，另一订阅者仍收到完整流式输出
- [x] 5.3 e2e：同一会话并发对话被 `-32004` 拒绝
- [x] 5.4 运行 `uv run pytest -v`、`uv run mypy src/`、`uv run ruff check src/ tests/` 全部通过

## 6. 文档同步

- [x] 6.1 更新 `docs/architecture.md`，补充读循环解耦、Run 生命周期与零订阅取消；人工核对
- [x] 6.2 更新 `AGENTS.md` 核心概念与项目结构（Run / 每会话单活跃 / 取消语义）；人工核对
